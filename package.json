{
  "name": "lightrag-root",
  "private": true,
  "scripts": {
    "build-frontend-and-stage": "cd lightrag_webui && npm install && npm run build && cd .. && git add lightrag/api/webui"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vite": "^7.0.5"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
